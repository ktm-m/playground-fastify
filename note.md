### TypeScript Command

#### Initial tsconfig.json

```bash
tsc --init
```

#### Compile TypeScript

```bash
tsc -p tsconfig.json
```

### NPM Options

- --save-dev or -D: Save as devDependencies
- --save or -S: Save as dependencies